import { WorksPage } from "../types/pages";
export declare const getWorksPage: (tagName: string) => Promise<WorksPage>;
export declare const getTagId: ($worksPage: WorksPage) => string;
export declare const getWorkUrl: ({ workId, chapterId, collectionName, }: {
    workId: string;
    chapterId?: string;
    collectionName?: string;
}) => string;
